@model PROYECTORADIX.Models.ProgramaCompetencium

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>ProgramaCompetencium</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="IdCompetencia" class="control-label"></label>
                <select asp-for="IdCompetencia" class ="form-control" asp-items="ViewBag.IdCompetencia"></select>
            </div>
            <div class="form-group">
                <label asp-for="IdPrograma" class="control-label"></label>
                <select asp-for="IdPrograma" class ="form-control" asp-items="ViewBag.IdPrograma"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
